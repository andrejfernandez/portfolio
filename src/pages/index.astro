---
import Layout from '../layouts/MainLayout.astro';
import Welcome from '../components/svelte/Welcome.svelte';
import { services } from '../utils/services';
import Card from '../components/ServiceCard.astro';
---

<Layout title="Home">
	<!-- Greeting -->
	<div id="home" class="min-h-screen flex flex-col items-center justify-center">
		<div class="relative z-30">
			<div class="flex items-center justify-between slide-in-left">
				<h1 class="text-5xl lg:text-8xl">Hi,</h1>
				<h1 class="text-5xl lg:text-8xl ml-4">I'm Andre <span class="wave z-10">👋</span></h1>
			</div>
			<p class="mt-2 ml-4 text-xl lg:text-3xl float-left slide-in-bottom">
				Full-Stack Web Developer!!
			</p>
		</div>
	</div>

	<!-- About -->
	<div
		id="about"
		class="min-h-full lg:min-h-screen flex flex-col md:flex-row items-center justify-center fade-in my-24 md:my-auto"
	>
		<h1 class="text-9xl mb-8 md:mb-0 mr-0 md:mr-14">👨‍💻</h1>
		<p class="w-5/6 md:w-1/2 text-lg md:text-xl">
			I'm a passionate full-stack web developer with a particular love for modern web technologies.
			My current front-end specialty lies in creating dynamic and user-friendly interfaces using <a
				class="underline"
				href="https://svelte.dev/"
				target="_blank">Svelte</a
			>,

			<a class="underline" href="https://kit.svelte.dev/" target="_blank">SvelteKit</a>, and
			<a class="underline" href="https://astro.build/" target="_blank">Astro</a>, which are
			cutting-edge frameworks for building highly interactive web experiences. My back-end specialty
			delves into the robust and scalable world of cloud computing. Leveraging platforms such as
			<a
				class="underline"
				href="https://azure.microsoft.com/en-us/products/app-service"
				target="_blank">Microsoft Azure</a
			>,
			<a class="underline" href="https://supabase.com/" target="_blank">Supabase</a>, and
			<a class="underline" href="https://www.netlify.com/" target="_blank">Netlify</a>, I design and
			implement flexible solutions that seamlessly adapt to business needs.
		</p>
	</div>

	<!-- Services -->
	<div
		id="services"
		class="min-h-screen flex flex-col md:flex-row items-center justify-start md:justify-center mb-24 md:mb-0"
	>
		{services.map((service) => <Card {...service} />)}
	</div>

	<!-- Contact -->
	<div
		id="contact"
		class="min-h-screen flex flex-col md:flex-row items-center justify-start md:justify-center mb-24 md:mb-0"
	>
		<h1>Contact</h1>
	</div>
</Layout>

<style>
	/* Animation */
	@keyframes slideInFromLeft {
		from {
			opacity: 0;
			transform: translateX(-20%);
		}
		to {
			opacity: 100;
			transform: translateX(0);
		}
	}
	@keyframes slideInFromBottom {
		from {
			opacity: 0;
			transform: translateY(50%);
		}
		to {
			opacity: 100;
			transform: translateY(0);
		}
	}
	@keyframes waveAnimation {
		0%,
		100% {
			transform: rotate(10deg);
		}
		50% {
			transform: rotate(-10deg);
		}
	}

	/* Class Style */
	.slide-in-left {
		opacity: 0;
		animation: slideInFromLeft 0.5s forwards ease-out;
	}
	.slide-in-bottom {
		opacity: 0;
		animation: slideInFromBottom 0.5s forwards ease-out;
		animation-delay: 0.5s;
	}

	.wave {
		display: inline-block;
		animation: waveAnimation 2s infinite;
		transform-origin: 70% 70%; /* Adjust this value to get the desired pivot point */
	}
</style>
